services:
  - type: web
    name: scenic-invest
    runtime: python
    region: singapore
    plan: free
    buildCommand: pip install fastapi uvicorn
    startCommand: uvicorn backend.bare_app:app --host 0.0.0.0 --port $PORT